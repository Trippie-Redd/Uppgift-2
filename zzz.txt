The website is for a video game development company, Pixlstream, and more specifically for their start page.
Change the HTML and CSS code according to the guidelines written.
1. DO NOT CHANGE HEADER, NAV OR FOOTER.
2. Add content to main and sidebar.
3. Try to keep the code simple and clean, as though it was written by a college student.
4. Use the vars provided to color the page.

Looks great!
Now style the form in the main box on the companys contact page.
You should also recolor the page using the color vars.
Once again try to keep the code simple and clean, as though it was written by a college student.

Now add a @media tag to the CSS to change the grid to the following when using a mobile phone:
    grid-template-columns: 1% 1fr 1%;
    grid-template-rows: auto;
    grid-template-areas: 
        ". header ."
        ". nav ."
        ". main ."
        ". sidebar ."
        ". footer .";


CSS:
:root {
    --primary-color: #8E9775;     /* Olivegreen */
    --secondary-color: #D9CAB3;   /* Beige */
    --accent-color: #4B4A67;      /* Dark purple */
    --background-color: #FCF7F8;  /* Creamwhite */
    --text-color: #2F2D2E;        /* Deepbrown */
}

body { 
    display: grid;

    max-height: 120vh;

    grid-template-columns: 1% 1fr 2fr 1%;
    grid-template-rows: 1fr 0.6fr 6fr 1fr;
    grid-template-areas: 
        ". header header ."
        ". nav nav ."
        ". sidebar main ."
        ". footer footer .";

    margin: 0;
    font-family: Arial, sans-serif;
    background-color: var(--background-color);
    color: var(--text-color);
}

header {
    grid-area: header;
    background-color: var(--primary-color);
    text-align: center;
}

.nav {
    grid-area: nav;
    background-color: var(--secondary-color);
    display: flex;
    justify-content: space-around;
    align-items: center;
    padding: 10px 0;
}

main {
    grid-area: main;
    background-color: var(--background-color);
    padding: 20px;
    border-radius: 8px;
    border: 1px solid var(--primary-color);
    display: flex;
    flex-direction: column;
    gap: 15px;
}

main h2 {
    color: var(--primary-color);
    text-align: center;
}

main form {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

main label {
    font-weight: bold;
}

main input, main textarea {
    padding: 10px;
    border: 1px solid var(--accent-color);
    border-radius: 4px;
    width: 100%;
    max-width: 500px;
}

main textarea {
    resize: vertical;
}

.radio-group {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
}

main button {
    background-color: var(--primary-color);
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 1em;
    width: fit-content;
    align-self: center;
}

main button:hover {
    background-color: var(--accent-color);
}

aside {
    grid-area: sidebar;
    background-color: var(--secondary-color);
    padding: 15px;
    border-radius: 8px;
    text-align: center;
}

aside img {
    width: 90%;
    border-radius: 8px;
    margin-top: 10px;
}

footer {
    grid-area: footer;
    background-color: var(--primary-color);
    color: white;
    padding: 20px 0;
    text-align: center;
}
